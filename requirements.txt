# Core
torch>=1.10.0
torchvision>=0.11.0
numpy>=1.21.0
pillow>=9.0.0

# Encoder backbone (required by the vendored segmentation_models_pytorch)
timm>=0.9.0

# Configuration and data
pyyaml>=6.0
pandas>=1.3.0
imageio>=2.16.0

# Annotation interface (PyQt5)
PyQt5>=5.15.0

# Visualization
matplotlib>=3.5.0

# Jupyter
jupyter>=1.0.0
ipykernel>=6.0.0
notebook>=6.4.0

# Progress bars
tqdm>=4.62.0

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0

# Optional — only needed to reproduce the CRF-based label-propagation baseline
# (Section 4.4.2 of the paper). Skip unless you want to run that comparison.
# pydensecrf>=1.0rc2

# Note: segmentation_models_pytorch is included locally (in this repo) and
# contains a modified version with the Error-Weighted Dice Loss (EWDL) and
# related loss functions. Do not install from PyPI — the local copy is the
# one the workflow uses.
